@startuml
skinparam sequence {
    LifeLineBackgroundColor #F5F5F5
    ParticipantBackgroundColor #FFFDD0
    ParticipantBorderColor #666666
}
skinparam NoteBackgroundColor #F5F5F5

title CU-016: Asignar actividades a alumnos

actor "Docente" as T
boundary "PanelActividadesView" as View
control "ApiService" as Api
control "ActivityController" as Ctrl
entity "AssignmentService" as Srv
entity "AssignmentRepository" as Repo
database "MySQL" as DB

autonumber

T -> View: Ingresa al panel de actividades
View -> Api: fetchAvailableActivities()
Api -> Ctrl: GET /api/activities/available
Ctrl -> Srv: getActivitiesForAssignment()
Srv -> Repo: findAllCreatedByTeachers()
Repo -> DB: SELECT * FROM activities
DB --> Repo: activityList
Repo --> Srv: activityList
Srv --> Ctrl: activityList
Ctrl --> Api: 200 OK
Api --> View: RenderList

T -> View: Selecciona actividad y opción "Asignar al grupo"
View -> Api: assignActivity(activityId, groupId)
Api -> Ctrl: POST /api/activities/assign (dto)
Ctrl -> Srv: processGroupAssignment(dto)

group Validación de Estado de Asignación (Backend)
    Srv -> Repo: existsActiveAssignment(activityId, groupId)
    Repo -> DB: SELECT id FROM assignments WHERE status='ACTIVE'...
    DB --> Repo: result
    
    alt #FFFDD0 La actividad ya está asignada en este momento
        Srv --> Ctrl: throw AlreadyAssignedException
        Ctrl --> Api: 409 Conflict ("Asignación duplicada")
        Api --> View: Error detectado
        View --> T: Mostrar: "La actividad ya se encuentra asignada al grupo"
    end
end

Srv -> Repo: saveAssignment(assignmentEntity)
Repo -> DB: INSERT INTO group_activity (activity_id, group_id, date)
DB --> Repo: success
Repo --> Srv: confirmation

Srv -> Srv: triggerNotificationService()
note right: El sistema notifica a los alumnos del grupo\nsobre la nueva tarea

Srv --> Ctrl: success
Ctrl --> Api: 201 Created
Api --> View: notifySuccess
View --> T: Notifica que la asignación fue enviada

@enduml