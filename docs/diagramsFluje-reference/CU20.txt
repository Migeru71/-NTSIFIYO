@startuml
skinparam sequence {
    LifeLineBackgroundColor #F5F5F5
    ParticipantBackgroundColor #FFFDD0
    ParticipantBorderColor #666666
}
skinparam NoteBackgroundColor #F5F5F5

title CU-020: Listar las tareas terminadas de los alumnos de un grupo

actor "Docente" as T
boundary "AsignacionesView" as View
control "ApiService" as Api
control "AssignmentController" as Ctrl
entity "AssignmentService" as Srv
entity "AssignmentRepository" as Repo
database "MySQL" as DB

autonumber

T -> View: Entra a la sección de asignaciones
View -> Api: fetchAssignedActivities()
Api -> Ctrl: GET /api/assignments/my-list
Ctrl -> Srv: getActivitiesCreatedByTeacher()
Srv -> Repo: findByTeacherId(currentUserId)
Repo -> DB: SELECT * FROM group_activity WHERE teacher_id = ?
DB --> Repo: listActivities
Repo --> Srv: listActivities
Srv --> Ctrl: listActivities
Ctrl --> Api: 200 OK
Api --> View: Renderizar lista de asignaciones

T -> View: Selecciona la actividad que necesite revisar
View -> Api: fetchAssignmentResults(activityId)
Api -> Ctrl: GET /api/assignments/{id}/results
Ctrl -> Srv: getDetailedResults(id)

group Obtención de Reportes y Desempeño
    Srv -> Repo: findResultsByAssignmentId(id)
    Repo -> DB: SELECT student, hits, time, errors FROM results WHERE ...
    DB --> Repo: dataResults / empty
    Repo --> Srv: dataResults
    
    alt #FFFDD0 Aun no hay respuestas de los alumnos
        Srv --> Ctrl: throw NoResponsesException
        Ctrl --> Api: 204 No Content (Empty)
        Api --> View: Estado vacío detectado
        View --> T: Mostrar: "Aún no hay respuestas de los alumnos"
    end
end

Srv --> Ctrl: DetailedResultsDto
Ctrl --> Api: 200 OK (List<StudentPerformance>)
Api --> View: Renderizar tabla de resultados
View --> T: Despliega listado de alumnos con aciertos, tiempo y errores

@enduml